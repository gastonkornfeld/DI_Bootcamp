

export const INCREASE_COUNT = () => {
    // 3. return to the reducer an object
    return{
      type:'ADD',
    }
}

export const DECREASE_COUNT = () => {
    // 3. return to the reducer an object
    return{
      type:'SUBSTRACT',
    }
}