
select COUNT(actor_id) FROM actorsTable;

INSERT INTO actorsTable VALUES(3, '','','1988-02-02', 4);

-- I tough it will be an error because is not null but it just added an empty value to them

select * from actorsTable;

